<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./stylesheets/style.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <a
        class="bg-red-500 float-right rounded-md px-3 py-2 text-sm inline-block mb-5"
        href="/logout"
        >Logout</a
      >
      <h5 class="mb-10">Edit Your Post.</h5>
      <form class="max-w-[75vh]" action="/update/<%= post._id%>" method="post">
        <textarea
          placeholder="What's on your mind ? "
          class="p-3 outline-none w-full h-[150px] resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        ><%= post.content %></textarea>
        <input
          class="text-sm cursor-pointer px-3 py-2 bg-yellow-500 block rounded-md w-full mt-4"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
